export function SideNavFooter (props){
    const {} = props ;
    return (
        <div className="sb-sidenav-footer">
                        <div className="small">Logged in as:</div>
                        Start Bootstrap
        </div>
    );
}